import tkinter as tk

# Create the main window
window = tk.Tk()
window.title("Basic Window")

# You might want to adjust the size
window.geometry("20x2000")  

# Start the event loop
window.mainloop()
